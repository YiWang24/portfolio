# ========================================
# Environment Variables for Cloudflare Zero Trust Proxy
# ========================================
# These variables are used server-side in API routes
# DO NOT prefix with NEXT_PUBLIC_ to keep them secure

# Cloudflare Zero Trust credentials (Get from Cloudflare Zero Trust Dashboard)
CF_CLIENT_ID=your-cloudflare-client-id
CF_CLIENT_SECRET=your-cloudflare-client-secret

# Backend API URL (internal, not exposed to client)
BACKEND_URL=https://api.yoursite.com

# ========================================
# Legacy variables (kept for backward compatibility)
# ========================================
# These are no longer used directly - requests go through our API proxy
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
# NEXT_PUBLIC_API_URL=http://localhost:8080
