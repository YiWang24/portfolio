# ========================================
# Public Environment Variables
# ========================================
# Site URL for metadata and SEO (update with your production domain)
NEXT_PUBLIC_SITE_URL=https://yoursite.com

# Docs URL for the navbar button (defaults to /docs)
NEXT_PUBLIC_DOCS_URL=https://docs.yoursite.com

# Backend API base (used by API proxy routes)
# Example: https://api.yoursite.com/api/v1
NEXT_PUBLIC_API_BASE_URL=https://api.yoursite.com/api/v1

# ========================================
# Server-Side Environment Variables
# ========================================
# These variables are used server-side in API routes
# DO NOT prefix with NEXT_PUBLIC_ to keep them secure

# Cloudflare Zero Trust credentials (Get from Cloudflare Zero Trust Dashboard)
CF_CLIENT_ID=your-cloudflare-client-id
CF_CLIENT_SECRET=your-cloudflare-client-secret

# Backend API URL (internal, not exposed to client)
# Used if NEXT_PUBLIC_API_BASE_URL is not set
BACKEND_URL=https://api.yoursite.com

# Docusaurus dev server URL for /docs rewrite
DOCUSAURUS_URL=http://localhost:3001
