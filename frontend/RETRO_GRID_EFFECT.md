# Retro Grid Background Effect

## 🎨 概述

为项目添加了 **Synthwave / Retro Grid** 3D 透视网格背景效果，营造出"终端悬浮在赛博空间上方"的视觉体验。

## 📦 新增文件

1. **`src/lib/utils.ts`**
   - 工具函数库
   - 包含 `cn()` 函数，用于合并 Tailwind CSS 类名

2. **`src/components/ui/RetroGrid.tsx`**
   - 核心网格组件
   - 纯 CSS/Tailwind 实现
   - 无需额外依赖

3. **依赖安装**
   - `clsx` - 条件类名工具
   - `tailwind-merge` - Tailwind 类名合并工具

## ✨ 视觉特性

### 3D 透视效果（中心向四周辐射）
- **透视类型**: 径向透视（Radial Perspective）
- **Perspective**: 1000px 视距，创造深度感
- **Rotate X**: 60度（向后倾斜）
- **Rotate Z**: 45度（旋转成菱形网格）
- **网格大小**: 60px × 60px 方格
- **位置**: 完全居中，从中心向四周边缘延伸
- **网格尺寸**: 200vmax × 200vmax（确保覆盖全屏）

### 颜色方案
- **灰绿色**: `rgba(16, 185, 129, 0.25)`
  - 与终端主题的绿色完美呼应
  - 适中的不透明度，清晰可见

### 动画效果
- **类型**: 背景位置从中心向外移动
- **时长**: 20秒缓慢移动
- **缓动**: Linear（匀速）
- **效果**: 模拟"在网格上向前飞行"

### 渐变遮罩（增强深度感）
- **中心**: 透明（远处清晰）
- **40% 处**: 开始暗化
- **70% 处**: 中等暗化
- **边缘**: 最暗（近处融入背景）
- **效果**: 中心远、边缘近的正方形透视感

## 🔧 技术实现

### CSS 核心技术

```css
/* 径向透视容器 */
perspective: 1000px;

/* 3D 网格变换 */
transform: rotateX(60deg) rotateZ(45deg);
transform-style: preserve-3d;

/* 居中定位 */
left: 50%;
top: 50%;
transform: translate(-50%, -50%);

/* 网格背景 */
background-image:
  linear-gradient(to right, rgba(16,185,129,0.25) 1px, transparent 0),
  linear-gradient(to bottom, rgba(16,185,129,0.25) 1px, transparent 0);
background-size: 60px 60px;

/* 径向渐变遮罩（中心远、边缘近）*/
background: radial-gradient(
  circle at center,
  transparent 0%,
  rgba(5,7,15,0.3) 40%,
  rgba(5,7,15,0.8) 100%
);

/* 动画：从中心向外 */
@keyframes grid-perspective {
  0% {
    background-position: center center;
  }
  100% {
    background-position: center calc(100% + 60px);
  }
}
```

### 组件层次

```
Background Layer (z-[-1])
├── Matrix Rain (z-[9999]) - 最顶层特效
├── 顶部圣光 - 紫色渐变
├── Retro Grid (z-[0]) - 3D 网格
├── 环境光晕 - 紫色 + 绿色
└── 静态网格 - 4rem 方格遮罩

Content Layer
└── Terminal - 主内容
```

## 🎨 颜色变体

如需调整网格颜色，可修改 `RetroGrid.tsx` 中的 `rgba` 值：

### 当前方案（灰绿色）
```css
rgba(16, 185, 129, 0.15)
```

### 推荐方案

**Subtle Grey（更低调）**
```css
rgba(255, 255, 255, 0.1)
```
- 更接近 Vercel/Next.js 官网风格
- 冷色调，专业感强

**Deep Purple（神秘感）**
```css
rgba(120, 119, 198, 0.3)
```
- 更有科技神秘感
- 与顶部圣光呼应

**Classic Matrix（经典黑客风）**
```css
rgba(0, 255, 0, 0.2)
```
- 纯绿色，更原汁原味
- 适合极客风格

## 📊 性能优化

- ✅ **纯 CSS 动画**: 使用 GPU 加速的 transform
- ✅ **pointer-events-none**: 不阻挡用户交互
- ✅ **will-change**: 浏览器自动优化
- ✅ **低透明度**: 减少视觉负担

## 🎯 使用场景

1. **Hero Section**: 为终端页面增加赛博朋克氛围
2. **Matrix 命令**: 配合 matrix 命令的数字雨效果
3. **整体风格**: 统一的科技感视觉语言

## 🚀 自定义建议

### 调整动画速度
```css
/* globals.css */
.animate-grid {
  animation: grid 15s linear infinite; /* 更快 */
  /* 或 */
  animation: grid 30s linear infinite; /* 更慢 */
}
```

### 调整网格密度
```tsx
/* RetroGrid.tsx */
[background-size:40px_40px]  /* 当前 */
[background-size:60px_60px]  /* 更稀疏 */
[background-size:20px_20px]  /* 更密集 */
```

### 调整倾斜角度
```tsx
[transform:rotateX(60deg)]  /* 当前：更陡峭 */
[transform:rotateX(45deg)]  /* 中等 */
[transform:rotateX(30deg)]  /* 更平缓 */
```

## 📝 集成要点

1. **位置**: 放在背景层的 z-[0]，在 Matrix Rain 之后
2. **层次**: 在静态背景色之上，内容之下
3. **不透明度**: 40% 保持低调
4. **pointer-events**: 确保不阻挡用户交互

## 🎉 最终效果

用户访问页面时会看到：
- ✨ 终端悬浮在 3D 网格地板上
- 🌊 网格缓慢向前移动（飞行感）
- 💫 远方网格自然消失在黑暗中
- 🎨 灰绿色与终端主题完美呼应
- 🚀 赛博朋克 / Synthwave 视觉风格
