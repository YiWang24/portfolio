{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "doppler run --project portfolio-web --config dev_personal -- concurrently \"npm run dev:frontend\" \"npm run dev:docs\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:docs": "cd docs && npm run start",
    "dev:backend": "doppler run --project portfolio-api --config dev -- mvn -f backend -Dmaven.sentry.plugin.skip=true spring-boot:run",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:backend\"",
    "build": "npm run build:frontend && npm run build:docs",
    "build:frontend": "cd frontend && npm run build",
    "build:docs": "cd docs && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../docs && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "workspaces": [
    "frontend",
    "docs"
  ]
}
